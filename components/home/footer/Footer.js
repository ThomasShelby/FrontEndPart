var React = require('react');
var ReactDOM = require('react-dom');

class Footer extends React.Component {
    render () {
        return (
            <footer className="container-fluid text-center">
                <p>Elect help description info</p>
            </footer>
        )
    }
}

module.exports = Footer;